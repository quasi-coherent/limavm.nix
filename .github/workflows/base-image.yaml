# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/base-image.yaml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
jobs:
  build-base-image:
    runs-on: ${{ matrix.runner }}
    steps:
    - uses: actions/checkout@v6
    - uses: cachix/install-nix-action@v30
      with:
        nix_path: nixpkgs=channel:nixos-unstable
    - uses: cachix/cachix-action@v17
      with:
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        name: limavm-nix
    - name: Build lima-base-image
      run: nix -Lv build .#packages.${{ matrix.system }}.lima-base-image
    strategy:
      matrix:
        include:
        - runner: ubuntu-24.04
          system: x86_64-linux
        - runner: ubuntu-24.04-arm
          system: aarch64-linux
'on':
  schedule:
  - cron: 0 0 * * 0
  workflow_dispatch: {}
